import React, { VFC } from "react";
import { ScrollView, Text } from 'react-native';

interface Props {

}

const Work:VFC<Props> = ({ }) => {
  return <ScrollView><Text>Work</Text></ScrollView>
}
export default Work;